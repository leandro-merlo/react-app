import React, { Component } from 'react';

export default class TodolistItem extends Component {

    constructor(props) {
        super(props);
    }

    render( ) {
        return (
            
        );
    }
} 